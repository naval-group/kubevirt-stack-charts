apiVersion: v2
name: hostpath-provisioner
description: HostPath Provisioner — local CSI storage for dev/test KubeVirt clusters
type: application
version: "0.2.0"
appVersion: "0.25.0"
kubeVersion: ">=1.30.0-0"
sources:
  - https://github.com/kubevirt/hostpath-provisioner-operator
