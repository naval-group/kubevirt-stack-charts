{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Delete Protection Helm Chart Values",
  "description": "Configuration values for the VM delete-protection webhook Helm chart.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "global": {
      "title": "Global Configuration",
      "description": "Global settings shared across all subcharts.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "imageRegistry": {
          "title": "Image Registry",
          "description": "Global container image registry override. When set, all images will be pulled from this registry.",
          "type": "string",
          "default": ""
        },
        "imagePullSecrets": {
          "title": "Image Pull Secrets",
          "description": "Global list of image pull secrets for authenticating to private registries.",
          "type": "array",
          "default": [],
          "items": {
            "type": "object"
          }
        },
        "nodeSelector": {
          "title": "Node Selector",
          "description": "Global node selector applied to all pods.",
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          }
        },
        "tolerations": {
          "title": "Tolerations",
          "description": "Global tolerations applied to all pods.",
          "type": "array",
          "default": [],
          "items": {
            "type": "object"
          }
        },
        "kubevirtNamespace": {
          "title": "KubeVirt Namespace",
          "description": "Global KubeVirt namespace. Used by subcharts that deploy into the KubeVirt namespace.",
          "type": "string",
          "default": "kubevirt"
        }
      }
    },
    "deleteProtection": {
      "title": "Delete Protection",
      "description": "Configuration for the VM delete-protection validating webhook.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "title": "Enabled",
          "description": "Whether to deploy the delete-protection webhook.",
          "type": "boolean",
          "default": true
        },
        "labelKey": {
          "title": "Label Key",
          "description": "Kubernetes label key used to mark VMs as protected from deletion.",
          "type": "string",
          "default": "kubevirt.io/delete-protection"
        },
        "labelValue": {
          "title": "Label Value",
          "description": "Label value that activates delete protection when present on a VM.",
          "type": "string",
          "default": "true"
        }
      }
    }
  }
}
