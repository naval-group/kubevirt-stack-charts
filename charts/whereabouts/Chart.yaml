apiVersion: v2
name: whereabouts
description: Whereabouts CNI IPAM plugin — cluster-wide IP address management for Kubernetes
type: application
version: "0.2.0"
appVersion: "0.9.4"
kubeVersion: ">=1.30.0-0"
sources:
  - https://github.com/k8snetworkplumbingwg/whereabouts
