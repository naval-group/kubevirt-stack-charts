{{- if not .Values.rke2.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: multus
  namespace: {{ .Values.namespace }}
  labels:
    app: multus
    {{- include "kubevirt-stack.labels" . | nindent 4 }}
{{- end }}
